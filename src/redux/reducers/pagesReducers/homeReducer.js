const initState = {};

const homeReducer = (state = initState ,action) => {
  
  switch(action.type){
    case "ACTION_TYPE" :
      return state;
    default :
      return state;
  }  
}

export default homeReducer;