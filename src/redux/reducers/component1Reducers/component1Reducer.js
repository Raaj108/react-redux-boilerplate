const initState = {};

const component1Reducer = (state = initState ,action) => {
  
  switch(action.type){
    case "ACTION_TYPE" :
      return state;
    default :
      return state;
  }  
}

export default component1Reducer;